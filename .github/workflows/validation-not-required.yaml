name: Quarkus Hilla Validation
on:
  pull_request_target:
    types: [opened, synchronize, reopened, edited]
    paths:
      - README.md
      - LICENSE
      - .gitignore
      - etc/**
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: "Validation not required"
        run: 'echo "✅ Pull request changes do not require validation"'